# Dependências do generator

`generator.constraints.txt` fixa o Copier e as ferramentas usadas para validar este repositório. Dependências da aplicação gerada vivem exclusivamente em `template/requirements/`.

Atualize versões em mudança dedicada, exercite as variantes com e sem Graph e revise os artefatos gerados antes de publicar uma tag.
