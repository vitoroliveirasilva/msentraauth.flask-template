# Microsoft Graph

Este arquivo existe somente quando `include_graph_example=true`. O exemplo `/profile` solicita `User.Read`, usa `$select` mínimo, bloqueia redirects, limita corpo, valida JSON/content-type, aplica timeout e retries limitados.
